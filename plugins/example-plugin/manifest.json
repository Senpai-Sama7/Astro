{
  "name": "example",
  "version": "1.0.0",
  "description": "Example plugin demonstrating the plugin system",
  "tools": [
    {
      "name": "greet",
      "description": "Returns a greeting message. Input: { name: string }",
      "handler": "greet"
    },
    {
      "name": "reverse",
      "description": "Reverses a string. Input: { text: string }",
      "handler": "reverse"
    }
  ]
}
